<%= form_for([:my, @project, @image]) do |f| %>

  <% if @image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<%= f.file_field :file %>
	<%= f.submit "Upload Image" %>
<% end %>

<% @images.each do |i| %>
	<div class = "image"> 
		<img src= "<%= i.file %>" />		
	</div>
<% end %>