<h1>Projects Index Page</h1>

<div class="projects">
<h2> Listing projects </h2>
<% @projects.each do |proj| %>
<%= render "showproject", :project => proj %>
<% end %>
</div>
<%= paginate @projects %>