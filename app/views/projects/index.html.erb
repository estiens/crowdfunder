<h1>Projects Index Page</h1>

<div class="projects">
<h2> Listing projects </h2>
<% @projects.each do |project| %>
<%= render project, :locals => {:project => project} %>
<% end %>
</div>
<%= paginate @projects %>